{
  "name": "gpt-chat-bot",
  "version": "3.3.0",
  "description": "**MinimalGPT** is an open-source GPT chat web app designed to be as self-contained as possible. All conversations are stored locally on the client's device, with the only information being sent to the server being API calls to GPT chat when the user sends a message and when a user saves a conversation to generate a conversation title.",
  "main": "index.js",
  "scripts": {
    "start-server": "http-server ./ -p 8080 --cors",
    "scss-build": "sass ./index.scss ./styles/site.css",
    "build": "gulp",
    "publish": "npm run scss-build && npm run build && firebase deploy",
    "publish-beta": "npm run scss-build && npm run build && firebase hosting:channel:deploy beta",
    "publish-all": "npm run scss-build && npm run build && firebase hosting:channel:deploy beta && firebase deploy"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fortawesome/fontawesome": "^1.1.8",
    "@fortawesome/fontawesome-free": "^6.4.0",
    "knockout": "^3.5.1",
    "markdown": "^0.5.0",
    "markdown-it-highlight": "^0.2.0",
    "swiped-events": "^1.1.7"
  },
  "keywords": [],
  "devDependencies": {
    "del": "^7.0.0",
    "http-server": "^14.1.1",
    "firebase": "^10.1.0",
    "sass": "^1.62.0",
    "gulp-filter": "^8.0.0",
    "gulp-rev": "^10.0.0",
    "gulp-rev-replace": "^0.4.4",
    "gulp": "^4.0.2",
    "gulp-html-replace": "^1.6.2",
    "gulp-if": "^3.0.0",
    "gulp-inject-string": "^1.1.2",
    "gulp-load-plugins": "^2.0.8",
    "gulp-newer": "^1.4.0",
    "gulp-rename": "^2.0.0"
  }
}
